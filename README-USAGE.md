# 🚀 Multi-Agent Growth Team - 使用说明

一个基于 AI SDK 的多 Agent 框架，模拟一个完整的增长团队协作，支持电商、博客和聊天室的自动化运营。

## 📋 目录

- [快速开始](#快速开始)
- [两种模式](#两种模式)
- [数据文件说明](#数据文件说明)
- [Agent 介绍](#agent-介绍)
- [CLI 命令](#cli-命令)
- [Web 界面](#web-界面)
- [示例对话](#示例对话)
- [API 接口](#api-接口)
- [扩展开发](#扩展开发)

---

## 🚀 快速开始

### 1. 安装依赖

```bash
pnpm install
```

### 2. 配置环境变量

```bash
cp .env.example .env
```

编辑 `.env` 文件：

```env
# Advanced mode (DeepSeek)
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Simple mode (OpenAI)
OPENAI_API_KEY=your_openai_api_key_here
```

### 3. 运行 CLI

**简单模式**（原版，使用 OpenAI）：
```bash
npm run agent
```

**高级模式**（增强版，使用 DeepSeek）：
```bash
npm run agent -- --advanced
# 或
npm run agent -- -a
```

### 4. 启动 Web 界面（可选）

```bash
npm run dev
```

访问：
- 主页：http://localhost:3000
- 聊天室：http://localhost:3000/chat
- 管理面板：http://localhost:3000/admin

---

## 🎭 两种模式

### 简单模式（Simple Mode）

- 使用 **OpenAI GPT-4o-mini**
- 3个简单 Agent：增长-A、增长-B、运营
- 通过 AI Tool 直接读写 JSON 文件
- 适合快速测试和演示

**启动方式**：
```bash
npm run agent
```

### 高级模式（Advanced Mode）

- 使用 **DeepSeek Chat**（成本更低）
- 4个专业 Agent：ContentCreator、DataAnalyst、ProductManager、CommunityManager
- 结构化的数据管理系统
- 支持实时聊天室集成（SSE）
- 持久化数据存储
- Web 管理面板

**启动方式**：
```bash
npm run agent -- --advanced
```

---

## 📁 数据文件说明

### `data/shop-mock.json` - 产品数据

```json
{
  "products": [
    {
      "id": "p1",
      "name": "商品名称",
      "price": 99,
      "description": "商品描述",
      "category": "分类",
      "stock": 100,
      "tags": ["标签1", "标签2"],
      "createdAt": 1234567890,
      "createdBy": "AgentName"
    }
  ]
}
```

### `data/blog-mock.json` - 博客文章

```json
[
  {
    "id": "b1",
    "title": "文章标题",
    "content": "文章内容",
    "excerpt": "摘要",
    "author": "作者",
    "tags": ["标签"],
    "publishedAt": 1234567890,
    "views": 100,
    "createdBy": "AgentName"
  }
]
```

### `data/chat-mock.json` - 聊天历史（静态）

用于展示历史消息，实际聊天室使用 `lib/chat-store.ts` 内存存储。

### `data/data.json` - UI 文案配置

存储首页、聊天室等页面的 UI 文案和配置。

---

## 🤖 Agent 介绍

### 高级模式的 4 个 Agent

#### 1. ContentCreator 📝 - 内容营销专家

**职责**：
- 创建博客文章
- 撰写产品描述
- 优化营销文案

**触发关键词**：
- "写博客"、"创建文章"、"内容营销"
- "产品描述"、"文案优化"

**示例**：
```
"为新产品写一篇博客文章"
"优化首页的营销文案"
```

#### 2. DataAnalyst 📊 - 数据分析师

**职责**：
- 分析用户行为
- 识别增长机会
- 提供数据洞察

**触发关键词**：
- "数据分析"、"用户行为"
- "增长机会"、"A/B测试"

**示例**：
```
"分析当前产品数据"
"找出最受欢迎的商品类别"
```

#### 3. ProductManager 🎯 - 产品经理

**职责**：
- 规划产品策略
- 创建新产品
- 优化产品组合

**触发关键词**：
- "产品策略"、"新产品"
- "产品优化"、"功能规划"

**示例**：
```
"创建3个新产品"
"规划下一季度的产品策略"
```

#### 4. CommunityManager 💬 - 社区经理

**职责**：
- 管理聊天室
- 用户互动
- 营造社区氛围

**触发关键词**：
- "聊天室"、"社区"
- "用户互动"、"发消息"

**示例**：
```
"在聊天室发几条欢迎消息"
"提升社区活跃度"
```

---

## 💻 CLI 命令

### 对话命令

直接输入文本与 Agent 团队对话：

```bash
👤 You: 创建5个科技产品
👤 You: 写3篇关于AI的博客文章
👤 You: 在聊天室发送欢迎消息
```

### 系统命令

| 命令 | 说明 |
|------|------|
| `agents` | 查看所有激活的 Agent |
| `stats` | 显示数据统计（产品数、文章数、消息数） |
| `products` | 列出所有产品 |
| `posts` | 列出所有博客文章 |
| `messages` | 显示最近的聊天消息 |
| `clear` | 清除对话历史 |
| `exit` / `quit` | 退出程序 |

---

## 🌐 Web 界面

### 1. 主页 `/`

展示 UI 组件库和功能演示

### 2. 聊天室 `/chat`

- 实时聊天功能（SSE）
- Agent 可以直接发送消息到聊天室
- 自动刷新，无需手动刷新

### 3. 管理面板 `/admin`

- 📊 实时统计面板
- 🛍️ 产品列表（标注 AI 创建）
- 📝 博客文章列表
- 💬 聊天消息流
- ⚡ 每 5 秒自动刷新

---

## 💡 示例对话

### 创建产品

```bash
👤 You: 为电商网站创建3个智能硬件产品

🤖 [ProductManager] 处理中...
   ✅ 决定响应！

🤖 ProductManager: 好的！我来创建3个智能硬件产品。

CREATE_PRODUCT: {"name": "智能手表 Pro", "description": "支持健康监测和通知的智能手表", "price": 299.99, "category": "智能硬件", "tags": ["智能", "健康", "可穿戴"]}

CREATE_PRODUCT: {"name": "无线耳机 Max", "description": "主动降噪无线耳机，续航30小时", "price": 199.99, "category": "智能硬件", "tags": ["音频", "无线", "降噪"]}

CREATE_PRODUCT: {"name": "智能音箱", "description": "支持语音助手的智能音箱", "price": 99.99, "category": "智能硬件", "tags": ["智能家居", "语音", "音频"]}

✅ Created product: 智能手表 Pro ($299.99)
✅ Created product: 无线耳机 Max ($199.99)
✅ Created product: 智能音箱 ($99.99)
```

### 创建博客文章

```bash
👤 You: 写一篇关于智能手表的博客文章

🤖 [ContentCreator] 处理中...
   ✅ 决定响应！

🤖 ContentCreator: 我来创建一篇引人入胜的博客文章！

CREATE_POST: {"title": "2026年智能手表选购指南", "content": "随着技术的发展，智能手表已经成为现代人不可或缺的配件...", "tags": ["智能硬件", "科技", "评测"]}

✅ Created blog post: "2026年智能手表选购指南"
```

### 聊天室互动

```bash
👤 You: 在聊天室发送5条热闹的消息

🤖 [CommunityManager] 处理中...
   ✅ 决定响应！

🤖 CommunityManager: 让我为聊天室增添活力！🔥

SEND_MESSAGE: {"user": "TechGeek", "text": "新品上架！智能手表Pro限时优惠！🎉"}
SEND_MESSAGE: {"user": "ShopBot", "text": "欢迎新朋友！有任何问题随时问我哦 ✨"}
...

✅ Sent chat message as TechGeek: "新品上架！..."
✅ Sent chat message as ShopBot: "欢迎新朋友！..."
```

---

## 🔌 API 接口

### GET `/api/agents?type={type}`

获取数据

**参数**：
- `stats` - 统计数据
- `products` - 产品列表
- `posts` - 博客文章列表
- `messages` - 聊天消息（可选 `&limit=50`）
- `all` - 所有数据

**示例**：
```bash
curl http://localhost:3000/api/agents?type=stats
curl http://localhost:3000/api/agents?type=products
curl http://localhost:3000/api/agents?type=messages&limit=20
```

### GET `/api/chat`

SSE 聊天流（实时聊天）

### POST `/api/chat`

发送聊天消息

**Body**：
```json
{
  "user": "用户名",
  "text": "消息内容"
}
```

---

## 🛠️ 扩展开发

### 创建新的 Agent

1. 在 `agents/growth-agents/` 创建新文件：

```typescript
import { BaseAgent } from '../base-agent';

export class MyNewAgent extends BaseAgent {
  constructor() {
    super({
      name: 'MyAgent',
      role: 'My Role Description',
      systemPrompt: `You are a specialized agent...`,
      temperature: 0.7,
    });
  }
}
```

2. 在 `agents/growth-agents/index.ts` 导出：

```typescript
export { MyNewAgent } from './my-new-agent';
```

3. 在 `cli/index-advanced.ts` 注册：

```typescript
coordinator.registerAgent(new MyNewAgent());
```

### 添加新的 Action 类型

1. 在 `agents/types.ts` 添加类型：

```typescript
export interface AgentAction {
  type: 'CREATE_PRODUCT' | 'CREATE_POST' | 'SEND_MESSAGE' | 'YOUR_NEW_ACTION';
  data: any;
}
```

2. 在 `agents/action-executor.ts` 实现：

```typescript
case 'YOUR_NEW_ACTION':
  return this.yourNewAction(action.data, agentName);
```

---

## 📊 架构说明

```
┌─────────────┐
│   CLI User  │
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│  Message Bus    │ ◄───┐
└────────┬────────┘     │
         │              │
         ▼              │
  ┌─────────────┐       │
  │ Coordinator │       │
  └──────┬──────┘       │
         │              │
    ┌────┴────┐         │
    ▼    ▼    ▼    ▼    │
   A1   A2   A3   A4    │
   │    │    │    │     │
   └────┴────┴────┴─────┘
         │
         ▼
  ┌──────────────┐
  │ Actions      │
  └──────┬───────┘
         │
    ┌────┴─────┐
    ▼          ▼
 ┌────────┐  ┌─────────┐
 │ Data   │  │ Chat    │
 │ Files  │  │ Store   │
 └────────┘  └─────────┘
```

---

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

---

## 📄 License

MIT
